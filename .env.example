# Sprout — environment placeholders
# NOTHING here is needed to run the app locally. The app runs fully on mock/localStorage data.
# These are the seams for when you go prototype -> real. See PROGRESS.md "How to swap mock -> real".

# --- Database + Auth (Supabase free tier) --- TODO: human to create project
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# --- Photo storage (Supabase Storage) --- TODO: same Supabase project, create a bucket "task-photos"
VITE_SUPABASE_STORAGE_BUCKET=task-photos

# --- Push notifications (web-push VAPID, OR Firebase Cloud Messaging) --- TODO: generate VAPID keys
VITE_VAPID_PUBLIC_KEY=

# --- Payments for Plus (Razorpay, India) --- TODO: only when monetizing
VITE_RAZORPAY_KEY_ID=

# --- Analytics (PostHog free tier / self-host Plausible) --- TODO: optional
VITE_POSTHOG_KEY=
